# Supabase Configuration
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key
DEFAULT_MODEL=gpt-4
MAX_TOKENS=150
TEMPERATURE=0.7

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_CACHE_TTL=3600
REDIS_L1_CACHE_SIZE=100
REDIS_L1_CACHE_TTL=300
SYNC_BATCH_SIZE=10
SYNC_INTERVAL=300

# Twilio Configuration
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG_MODE=False

# Scheduler Configuration
SCHEDULER_CHECK_INTERVAL=60  # Intervalo en segundos para revisar campañas
SCHEDULER_MAX_CONCURRENT_CALLS=10  # Máximo de llamadas simultáneas
SCHEDULER_RETRY_DELAY=15  # Tiempo en minutos entre reintentos por defecto
SCHEDULER_DEFAULT_MAX_RETRIES=3  # Número máximo de reintentos por defecto

# Call Configuration
CALL_TIMEOUT=30  # Tiempo máximo de espera para una llamada en segundos
CALL_DEFAULT_WEBHOOK_BASE_URL=your_default_webhook_base_url  # URL base para webhooks de llamadas
CALL_DEFAULT_FROM_NUMBER=your_default_from_number  # Número de teléfono por defecto para realizar llamadas

# Security
JWT_SECRET=your_jwt_secret
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database
DATABASE_URL=your_database_url


# AI Service Configuration
OPENAI_API_KEY=your_openai_api_key
AI_MODEL=gpt-4
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=150

# Conversation Settings
MAX_CONVERSATION_HISTORY=10
CONVERSATION_TIMEOUT_MINUTES=30
